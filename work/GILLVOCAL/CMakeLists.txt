cmake_minimum_required(VERSION 3.22)
# Universal macOS defaults are set before compiler detection; CI may request
# one architecture explicitly and combine independently tested slices later.
if(CMAKE_HOST_APPLE OR CMAKE_SYSTEM_NAME STREQUAL "Darwin")
    if(NOT DEFINED CMAKE_OSX_ARCHITECTURES OR CMAKE_OSX_ARCHITECTURES STREQUAL "")
        set(CMAKE_OSX_ARCHITECTURES "arm64;x86_64" CACHE STRING "macOS architectures")
    endif()
    if(NOT DEFINED CMAKE_OSX_DEPLOYMENT_TARGET OR CMAKE_OSX_DEPLOYMENT_TARGET STREQUAL "")
        set(CMAKE_OSX_DEPLOYMENT_TARGET "11.0" CACHE STRING "Minimum supported macOS")
    endif()
endif()
project(GILLVOCAL VERSION 0.4.0 LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
# Apple Clang contracts float multiply/add by default. JUCE's decimal
# parameter-grid snapping must retain the same rounding steps as Windows:
# e.g. -12 + 0.01f * 1200 must be exactly 0 dB, not an FMA residual.
# Keep IEEE rounding boundaries for control/state and DSP code consistently.
if(APPLE AND CMAKE_CXX_COMPILER_ID MATCHES "Clang")
    add_compile_options(-ffp-contract=off)
endif()
if(MSVC)
    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
# These two optional shortcuts reference the local Windows JUCE tools/runtime.
# macOS always compiles the pinned JUCE sources for the requested architecture.
if(NOT WIN32 AND (GILL_JUCE_EXPORT OR GILL_PREBUILT_RUNTIME))
    message(FATAL_ERROR "GILL_JUCE_EXPORT and GILL_PREBUILT_RUNTIME are Windows-only. Use a clean macOS build directory without these options.")
endif()
set(GILL_JUCE_SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../dependencies/JUCE" CACHE PATH "Pinned JUCE source directory")

set(JUCE_BUILD_EXTRAS OFF CACHE BOOL "" FORCE)
set(JUCE_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
if(GILL_JUCE_EXPORT)
  include("${GILL_JUCE_EXPORT}")
else()
  add_subdirectory("${GILL_JUCE_SOURCE_DIR}" JUCE)
endif()
set(runtime_defs JUCE_WEB_BROWSER=0 JUCE_USE_CURL=0 JUCE_USE_WIN_WEBVIEW2=0 JUCE_REPORT_APP_USAGE=0 JUCE_GLOBAL_MODULE_SETTINGS_INCLUDED=1)
foreach(m audio_basics audio_devices audio_formats audio_processors audio_processors_headless audio_utils core data_structures events graphics gui_basics gui_extra)
  list(APPEND runtime_defs JUCE_MODULE_AVAILABLE_juce_${m}=1)
endforeach()
if(WIN32 AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Tests/Vst3HostTests.cpp")
  add_executable(GillVocalVst3HostTests Tests/Vst3HostTests.cpp)
  target_link_libraries(GillVocalVst3HostTests PRIVATE GillVocalRuntime juce::juce_audio_processors)
  target_include_directories(GillVocalVst3HostTests PRIVATE Source)
  target_compile_definitions(GillVocalVst3HostTests PRIVATE JUCE_PLUGINHOST_VST3=1 ${runtime_defs})
  target_sources(GillVocalVst3HostTests PRIVATE
    "${JUCE_MODULES_DIR}/juce_audio_processors/juce_audio_processors.cpp"
    "${JUCE_MODULES_DIR}/juce_audio_processors_headless/juce_audio_processors_headless.cpp")
endif()
if(GILL_PREBUILT_RUNTIME)
  add_library(GillVocalRuntime STATIC IMPORTED)
  set_target_properties(GillVocalRuntime PROPERTIES IMPORTED_LOCATION "${GILL_PREBUILT_RUNTIME}")
  target_include_directories(GillVocalRuntime INTERFACE "${JUCE_MODULES_DIR}")
  target_compile_definitions(GillVocalRuntime INTERFACE ${runtime_defs})
  target_link_libraries(GillVocalRuntime INTERFACE juce::juce_recommended_config_flags winmm version shlwapi Imm32 comdlg32 ole32 oleaut32 uuid wininet ws2_32)
  # Local builds reuse the exact pinned neutral JUCE runtime. Keep module include
  # paths/definitions/system libraries but do not compile its sources per plugin.
  foreach(m audio_basics audio_devices audio_formats audio_processors audio_processors_headless audio_utils core data_structures events graphics gui_basics gui_extra)
    if(TARGET juce_${m})
      set_property(TARGET juce_${m} PROPERTY INTERFACE_SOURCES "")
    endif()
  endforeach()
else()
  add_library(GillVocalRuntime STATIC Source/JuceRuntime.cpp)
  target_compile_definitions(GillVocalRuntime PUBLIC ${runtime_defs})
  target_link_libraries(GillVocalRuntime PRIVATE juce::juce_audio_utils PUBLIC juce::juce_recommended_config_flags)
endif()
juce_add_binary_data(GillVocalAssets SOURCES Assets/core_reference.png)
add_library(GillVocalShared STATIC Source/PluginProcessor.cpp Source/PluginEditor.cpp)
target_link_libraries(GillVocalShared PRIVATE GillVocalRuntime GillVocalAssets)
target_include_directories(GillVocalShared PUBLIC Source "$<TARGET_PROPERTY:GillVocalRuntime,INTERFACE_INCLUDE_DIRECTORIES>" "${JUCE_MODULES_DIR}")
target_compile_definitions(GillVocalShared PUBLIC ${runtime_defs} "$<TARGET_PROPERTY:GillVocalRuntime,INTERFACE_COMPILE_DEFINITIONS>")
function(add_gill product code kind category)
  string(TOLOWER "${product}" lower)
  set(display_name "${product}")
  if(ARGC GREATER 4)
    set(display_name "${ARGV4}")
  endif()
  juce_add_plugin(${product} VERSION ${PROJECT_VERSION} COMPANY_NAME "GILLPRODUCTION"
    BUNDLE_ID "com.gillproduction.${lower}" PLUGIN_MANUFACTURER_CODE Gill PLUGIN_CODE ${code}
    PRODUCT_NAME "${display_name}" DESCRIPTION "${display_name} Vocal Processing"
    FORMATS VST3 VST3_CATEGORIES Fx ${category} VST3_AUTO_MANIFEST FALSE
    IS_SYNTH FALSE NEEDS_MIDI_INPUT FALSE NEEDS_MIDI_OUTPUT FALSE IS_MIDI_EFFECT FALSE
    EDITOR_WANTS_KEYBOARD_FOCUS TRUE COPY_PLUGIN_AFTER_BUILD FALSE)
  if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
    get_target_property(generated ${product} JUCE_GENERATED_SOURCES_DIRECTORY)
    get_target_property(info ${product} JUCE_INFO_FILE)
    add_custom_command(OUTPUT "${generated}/${product}_resources.rc" APPEND DEPENDS "${info}")
  endif()
  target_sources(${product} PRIVATE Source/Factory.cpp)
  target_compile_definitions(${product} PUBLIC GILL_VOCAL_KIND=${kind} JUCE_VST3_CAN_REPLACE_VST2=0)
  target_link_libraries(${product} PRIVATE GillVocalShared GillVocalAssets PUBLIC juce::juce_recommended_config_flags)
endfunction()
add_gill(GILLFLOW Gfl1 0 Dynamics)
add_gill(GILLHEAT Ght1 1 Distortion)
add_gill(GILLTUNE Gtu1 2 "Pitch Shift")
add_gill(GILLTUNELIVE Gtl1 2 "Pitch Shift" "GILLTUNE LIVE")
target_compile_definitions(GILLTUNELIVE PUBLIC GILL_TUNE_LIVE=1)
enable_testing()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Tests/PluginTests.cpp")
  add_executable(GillVocalIntegrationTests Tests/PluginTests.cpp)
  target_link_libraries(GillVocalIntegrationTests PRIVATE GillVocalShared GillVocalAssets)
  add_test(NAME VOCAL_INTEGRATION COMMAND GillVocalIntegrationTests)
  set_tests_properties(VOCAL_INTEGRATION PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Tests")
endif()
foreach(engine Flow Heat Tune)
  add_executable(Gill${engine}DspTests Tests/${engine}Tests.cpp)
  target_include_directories(Gill${engine}DspTests PRIVATE Source)
  add_test(NAME ${engine}_DSP COMMAND Gill${engine}DspTests)
  set_tests_properties(${engine}_DSP PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Tests")
endforeach()
